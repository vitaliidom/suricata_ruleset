#alert tcp $LOLKA_SERVER any ->  $TELEPORT_SERVER any (msg:"TCP traffic to $TELEPORT_SERVER detected"; sid:1000001; rev:1;)

alert udp $LOLKA_SERVER any ->  $TELEPORT_SERVER !$DNS_PORTS (msg:"UDP traffic to $TELEPORT_SERVER  detected"; sid:1000002; rev:1;)

alert icmp $LOLKA_SERVER any -> $TELEPORT_SERVER any (msg:"ICMP traffic to $TELEPORT_SERVER detected"; sid:1000003; rev:1;)

alert udp $DNS_SERVERS !$DNS_SERVER_PORTS -> !$DNS_ALLOWED_SERVERS !$DNS_SERVER_PORTS (msg:"UDP traffic from $DNS_SERVERS detected"; sid:1000004; rev:1;)

alert tcp $DNS_SERVERS !$DNS_SERVER_PORTS -> !$DNS_ALLOWED_SERVERS !$DNS_SERVER_PORTS (msg:"TCP traffic from $DNS_SERVERS detected"; sid:1000005; rev:1;)

alert icmp $DNS_SERVERS any -> !$ZABBIX_SERVERS any (msg:"ICMP traffic from $DNS_SERVERS detected"; sid:1000006; rev:1;)

alert udp $STATUS_SERVERS any -> any any (msg:"UDP traffic from $STATUS_SERVERS detected"; sid:1000004; rev:1;)

alert tcp $STATUS_SERVERS any -> any any (msg:"TCP traffic from $STATUS_SERVERS detected"; sid:1000005; rev:1;)

alert icmp $STATUS_SERVERS any -> any any (msg:"ICMP traffic from $STATUS_SERVERS detected"; sid:1000006; rev:1;)
